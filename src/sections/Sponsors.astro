---
const SPONSORS = [
  {
    id: "Stefy Food",
    nombre: "Stefy Food",
    logo: "https://stefyfood.vercel.app/images/logo_stefy_food.png",
    url: "https://stefyfood.vercel.app/",
  },
  {
    id: "RosaPastell",
    nombre: "RosaPastell",
    url: "https://www.rosapastell.com",
    logo: "/img/rosapastell-logo.png",
  },
 
 
];
let COLS = SPONSORS.length > 2 ? 3 : 2;
console.log(COLS);
---

<sectio class="block mt-80 mb-5">
  <h3 class="text-secondary text-2xl text-center mb-6 font-semibold">Patrocinadores</h3>
  <div class={`grid grid-cols-2 md:grid-cols-${COLS} gap-8`}>
    {
      SPONSORS.map(({ id, nombre, url, logo }) => (
        <a
          href={url}
          target="_blank"
          rel="noopener noreferrer"
          title={`Visita la página del patrocinador ${nombre}`}
          class="flex justify-center items-center bg-gradient-to-b from-white/10 via-transparent to-transparent p-2"
        >
          <img
            loading="lazy"
            src={logo}
            alt={nombre}
            class="w-20 rounded-full hover:opacity-70 hover:scale-105 transition"
          />
        </a>
      ))
    }
  </div>
</sectio>
