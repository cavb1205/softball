---
const SPONSORS = [

  {
    id: "RosaPastell",
    nombre: "RosaPastell",
    url: "https://www.rosapastell.com",
    logo: "/img/patrocinadores/rosapastell-logo.png",
  },
  {
    id:"DivaStore",
    nombre: "DivaStore",
    url: "https://www.instagram.com/divastore_cl/",
    logo: "/img/patrocinadores/divastore-logo.webp", 
  }
 
  
 
 
];
let COLS = SPONSORS.length > 2 ? 3 : 2;

---

<sectio class="block mt-40 mb-10">
  <h3 class=" text-2xl text-center mb-6 font-semibold ">Patrocinadores</h3>
  <div class="flex flex-col md:flex-row md:flex-wrap justify-around gap-8">
    {
      SPONSORS.map(({ id, nombre, url, logo }) => (
        <a
          href={url}
          target="_blank"
          rel="noopener noreferrer"
          title={`Visita la página del patrocinador ${nombre}`}
          class="flex justify-center items-center bg-gradient-to-b from-white/10 via-transparent to-transparent p-2"
        >
          <img
            loading="lazy"
            src={logo}
            alt={nombre}
            class="w-20 rounded-full hover:opacity-70 hover:scale-105 transition mx-20"
          />
        </a>
      ))
    }
  </div>
</sectio>
